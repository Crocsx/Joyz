import React from 'react';
import {
  RouteProps, Redirect
} from 'react-router-dom'

const List = (props: RouteProps): JSX.Element => {
  const authKey = localStorage.getItem("auth_key");
  return (
    <div>

    </div>
  )
}

export default List;